while True:
    binary=input("Enter a binary value, only '1' or '0', lower or equal to 8 bit")
    if len(binary)>8:
        print("you didnt enter a binary that is 8 bits or lower")
    else:
        #binary="binaryconversion"
        binary = list(binary)
        print(binary)
        number = 0

        #list=["start","eng","inc"]
        #binary[start:eng:incrementvalue]
        value=(binary[::-1])
        #print("value")
        power=len(binary)-1
        power2=2**power
        #print(power2)
        length=len(binary)
        #print(value)
        for i in range(0,length):
            if binary[i] == "1":
                number=number+power2
                power=power-1
                power2=2**power
            else:
                power=power-1
                power2=2**power
        print(number)
        break
