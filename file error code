### create text file
### use code to check the file
### count error and put in empty dictionary
## counts error on each line
### output number of error to user

error=0
try:
  my_file = open("file.txt", "r")
  for i in my_file:
    ## it wont show as error, it would be like 
    ## "2026-08-01 ERROR Database timeout\n"
    if "error" in i.lower():
     error+=1

  my_file.close()

except:
  my_file = open("file.txt", "w")
  my_file.write("2026-08-16 ERROR Database timeout\n")
  my_file.close()
  print("file was missing, so I created it for you! Run the script again.")


print(f"there were {error} errors")
