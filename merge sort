def merge_sort(arr):

  if len(arr) <= 1:
        return arr

  len_original=len(arr)
  half_original=len(arr)//2
  sorted1=merged(arr[0:half_original])
  sorted2=merged(arr[half_original:len(arr)])

  return merge(list1,list2)

def merged(list):







list=input("Give me a list and seperate with ,")
list=list.split(",")
list=list(list)

merge_sort(list)
