def merge_sort(arr):
  len_original=len(arr)
  half_original=len(arr)//2
  list1=arr[0:half_original]
  list2=arr[half_original:len(arr)]
  





list=input("Give me a list and seperate with ,")
list=list.split(",")
list=list(list)

merge_sort(list)
