def binary_search(l,value):
    l=list[list_input]
    index=(len(l))//2
    w=l[index]
    if int(w)>value:
        l=l[0:index]
        binary_search(l,value)
    elif int(w)<value:
        l=l[index:len(l)]
        binary_search(l,value)
    else:
        print("found")
        return binary_search(l,value)

list_input=input("Enter the list")
value=int(input("Enter the number"))
binary_search(list_input,value)
        

