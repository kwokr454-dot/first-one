# data type tuple() is locked forever unlike list(), whcih can be cahnged

tuple1=("Monday","Tuesday","Wednesday","Thursday","Friday","Saturday","Sunday")

##the use of () instead of [], makes it a tuple, whcih is parentheses,
#  [] is a list whcih can be mutated

for i, x in enumerate(tuple1):
    print(f"{i+1}. {x}")

#for i in range(len(tuple1)):
#    print(f"{i+1} is {tuple1[i]}")

try:
    tuple1.append("try")
    print(tuple1)
except:
    print("doesnt work")


#it would output doesnt work cuz its a tuple
