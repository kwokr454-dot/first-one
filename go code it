# Remember - you must provide the correct subroutine name below and any parameters requested
# complete the code for the subroutine below

def checkScore(a):
  for i in range(1,len(a)):
    a=1
    if int(a[i]) == (int(a[i-1])+1):
      a+=1
      if a==5:
        return True
    else:
      a = 1
  return False
